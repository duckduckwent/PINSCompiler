typ log : logical;
typ int : integer;

fun test (n : int) : int = (
    {
        for i=0, n, 1:
            { table[n][i] = n * (2 % 3) }
    },
    print_int(table[n][2])
)
{ where
    var table : arr[5] arr[5] int;
    var i : int
};

fun main (argc : integer) : int = (
    test(4)
)
